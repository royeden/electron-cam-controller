import Camera from "../components/Camera2";

const Home = () => {
  return (
    <main className="flex flex-col items-center justify-center h-screen text-white bg-dark">
      <Camera />
    </main>
  );
};

export default Home;
